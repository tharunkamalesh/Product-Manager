apiVersion: v1
kind: Service

metadata:
  name: ai-saas-service

spec:
  type: NodePort

  selector:
    app: ai-saas

  ports:
    - protocol: TCP
      port: 80
      targetPort: 4173
      nodePort: 30007